# React Report Template

新报告建议从统一模板开始，而不是每次从零搭一个仓库。

建议约定：

- `src/content/`: 报告内容与引用资料。
- `src/slides/`: 页面组件。
- `src/shared/`: 可复用图表、卡片、脚注、导航。
- `public/media/`: 图片、视频、截图。
- `dist/`: 汇报完成后的静态输出。

新增报告后，把目录加入根目录 `report-hub.config.json`。
